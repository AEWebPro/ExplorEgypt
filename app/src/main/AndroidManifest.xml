<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ae.smartvisit">

    <application
        android:name=".infrastructure.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.Services" />
        <activity android:name=".activities.DetailView" />
        <activity android:name=".activities.Transport" />
        <activity android:name=".activities.Hospitals" />
        <activity android:name=".activities.Currency" />
        <activity android:name=".activities.Embassy" />
        <activity android:name=".activities.Planes" />
        <activity android:name=".activities.YourPlanes" />
        <activity android:name=".activities.RecommandA" />
        <activity android:name=".activities.RecommandPrograms" />
        <activity android:name=".activities.YourPlanesA"></activity>
        <activity android:name=".activities.Signup"></activity>
    </application>

</manifest>